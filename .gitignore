# Virtual environments
.venv/
venv/

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Secrets & local config
.env
.env.*
!.env.example  # tracked: secret-free template, copied to .env locally

# Data, models, and indexes (published to HF Hub, never committed)
data/
models/
qdrant_storage/
*.onnx
*.gguf

# Work-in-progress generated eval set (commit deliberately with `git add -f`
# once it is a complete, reviewed silver set — see project plan).
eval_data/generated_evalset.jsonl
eval_data/*.processed

# Deterministic train/test split of the silver set (reproducible from the seed
# via scripts/split_evalset.py — no need to commit the derived partitions).
eval_data/silver_train.jsonl
eval_data/silver_test.jsonl

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
